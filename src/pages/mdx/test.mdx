import Cells from "./cells.jsx"

# MDX Test page

MDX allows for React components to be used inside a markdown file.

## Responsive cells

<Cells />

In this case the cells have a minimum width of 250px. Apart from that they adapt to the width of the page.
